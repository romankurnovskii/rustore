#!/usr/bin/env sh

# lint-staged проверяет и исправляет только измененные файлы
# (включает eslint --fix и prettier --write для staged файлов)
npx lint-staged

# Быстрая проверка типов TypeScript
npm run type-check
